'use strict';

import 'isomorphic-fetch';
import 'leaflet-routing-machine';
const $ = jQuery;
const $document = $( document );
const $window = $( window );
const Promise = require( 'bluebird' );

import './advanced-custom-fields.js';
